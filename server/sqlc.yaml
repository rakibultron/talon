version: "2"
sql:
  - engine: "postgresql"
    queries: "./db/queries" # will refer a file
    schema: "./db/migrations"
    gen:
      go:
        package: "dbops"
        sql_package: "pgx/v5"
        out: "./db/dbops"
